# ????
server.port=8080

# MySQL ?????
spring.datasource.url=jdbc:mysql://localhost:3306/kp_paper_cutting?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=utf8&useUnicode=true
spring.datasource.username=${DB_USERNAME:paper_user}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ??????
spring.servlet.multipart.max-file-size=300MB
spring.servlet.multipart.max-request-size=320MB

# JWT
jwt.secret=${JWT_SECRET:}
jwt.expiration-seconds=604800

# App update metadata
app.update.latest-version-code=1
app.update.latest-version-name=1.0
app.update.apk-url=http://49.232.151.194/download/app-release.apk
app.update.release-notes=修复已知问题，优化使用体验。
app.update.force-update=false
